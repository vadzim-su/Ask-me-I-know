import { FilterQuestionsPipe } from './filter.pipe';

describe('FilterQuestionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterQuestionsPipe();
    expect(pipe).toBeTruthy();
  });
});
